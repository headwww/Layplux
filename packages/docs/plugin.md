# 插件系统

::: warning 开发中
插件系统正在设计中，预计在后续版本发布。
:::

## 设计目标

- 每个插件有独立的事件命名空间
- 支持通配符跨插件通信
- DAG 依赖排序
- 动态注册/卸载

## 事件总线

插件系统将基于已集成的 EventEmitter2 事件总线：

```ts
// 基础 API 已可用
skeleton.event.onGlobal('**', (payload) => {
  // 监听所有全局事件
})
```
